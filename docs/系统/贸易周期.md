# 贸易周期

全局统一的库存刷新节律。

---
创建日期: 2026-04-28
最后更新: 2026-05-14
版本: v1.2
状态: 已确定
关联系统: 村庄库存规则
---

## 全局统一

所有村庄使用同一刷新周期。

## 配置项

| 配置项 | 字段名 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| 时间模式 | `time_mode` | 枚举 | `server_time` | 可选：`server_time`（服务器运行时间）/ `game_time`（游戏内昼夜循环） |
| 周期长度 | `cycle_days` | 整数 | 3 | 建议范围 1-30，单位为游戏日 |
| 刷新时刻 | `refresh_time` | 枚举 | `sunrise` | 可选：`sunrise` / `noon` / `sunset` / `midnight` |
| 恢复规则 | `restore_mode` | 枚举 | `category` | 可选：`full`（全量恢复）/ `category`（分类处理）/ `incremental`（增量恢复） |

### 时间模式说明

| 模式 | 说明 |
| :--- | :--- |
| `server_time` | 基于服务器累计运行时间（`gameTime`），服务器运行时间推进则周期推进 |
| `game_time` | 基于游戏内昼夜循环（`dayTime`），与游戏内实际经历的昼夜一致 |

恢复规则的具体参数（如分类处理的各档比例、增量恢复的百分比）待后续定义。

## 首次刷新（固定逻辑）

世界生成时，所有村庄库存直接应用基准值。

第一个周期自此刻开始计时。

此逻辑不可配置。

## 流转动力

库存为 0 时，不等刷新就出发——这是玩家继续上路的最自然理由。

---

## 相关

- [村庄库存规则](村庄库存规则.md) - 单村库存上限与消耗
- [贸易周期实现](../技术/贸易周期实现.md) - 技术实现
